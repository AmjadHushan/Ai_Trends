# 🎬 وكيل الإنتاج والربط البرمجي الموحد (AI Production Agent Specification)

## 📌 1. المهام الأساسية لخط الإنتاج (Core Pipeline Responsibilities)

يتولى وكيل الإنتاج إدارة وتنفيذ عمليات المونتاج وتوليد الوسائط بالتتابع الصارم وفق الخطوات التالية:
- [ ] **توليد الصوت (Audio Generation):** استدعاء محرك `Edge-TTS` لتحويل النص المعتمد من السيناريو إلى ملفات صوتية تعليقية عالية الجودة.
- [ ] **تحريك الأفاتار (Lip-Sync & Avatar):** معالجة ملف الصوت عبر موديل الـ Lip-sync لمزامنة حركة شفاه الأفاتار المعتمد رقمياً.
- [ ] **دمج اللقطات (Footage Stitching):** سحب وجلب اللقطات المرئية والخلفيات بناءً على الكلمات المفتاحية الذكية المستلمة من وكيل الإدارة.
- [ ] **الرندرة النهائية (Final Rendering):** دمج وتجميع كافة طبقات الفيديو والصوت والموسيقى لإنتاج المخرج النهائي باستخدام مكتبة `MoviePy` وأوامر `FFmpeg`.

---

## 🛠️ 2. محرك التعديل الموضعي الذكي (Granular Patching Engine)

عند استلام أمر التعديل والملاحظات `CMD_TELEGRAM_PATCH` من بوت التليجرام، يجب على الوكيل تخطي عملية الإنتاج الكاملة، وتفعيل **"الرندرة الجزئية الذكية"** بناءً على نوع الطلب لتوفير وقت السيرفر:

### 🔇 الحالة الأولى: إلغاء الموسيقى الخلفية
* **الأمر البرمجي:** `PATCH:AUDIO:REMOVE_BGM`
* **الآلية:** الاحتفاظ بملف التعليق الصوتي الأساسي المستخرج من `Edge-TTS` كما هو، مع إلغاء طبقة الموسيقى الخلفية تماماً من الكود، وإعادة تشغيل `FFmpeg` لدمج الصوت البشري مع اللقطات المرئية مباشرة.

### 🗣️ الحالة الثانية: تغيير المعلق الصوتي
* **الأمر البرمجي:** `PATCH:AUDIO:SWAP_VOICE`
* **الآلية:** إرسال إشارة عكسية لوكيل الكتابة `AI_Writer_Agent` لتحديث معرف الصوت (Voice Seed)، وإعادة توليد ملف الصوت فقط عبر `Edge-TTS`. يتم الاحتفاظ بجميع اللقطات المرئية القديمة ودمج الصوت الجديد معها.

### 👤 الحالة الثالثة: تغيير الأفاتار بالكامل
* **الأمر البرمجي:** `PATCH:VIDEO:SWAP_AVATAR`
* **الآلية:** قفل وتثبيت ملف الصوت المولد بنجاح، جلب مسارات الأفاتار الجديد من الإعدادات، وإعادة تشغيل موديل الـ `Lip-sync` على الأفاتار الجديد فقط، ثم تركيبه فوق الفيديو النهائي وإعادة الرندرة.

---

## 🔄 3. توجيه المخرجات النهائية (Final Pipeline Routing)

* فور انتهاء أي عملية رندرة (سواء كانت أساسية أو تعديل موضعي)، يتم توليد معرف فريد للمشروع `Project_ID` ومعرف للفيديو `video_id`.
* يتم إرسال ملف الفيديو النهائي التابع للمشروع مباشرة إلى واجهة التحكم `budget_telegram_interface.txt` لانتظار قرار المطور البشري.
