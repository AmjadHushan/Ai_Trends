# مواصفات موديول النشر والرفع التلقائي المتعدّد (Omni-Publishing API Gate)

## ⚙️ 1. المعالجة والرفع البرمجي (Upload Pipeline)
بعد انتهاء "وكيل الإنتاج" من المونتاج ورندرة الفيديو بواسطة MoviePy، يتلقى موديول النشر إشارة البدء محملة بمسار الملف النهائي. يتم الرفع عبر المسارات التالية:
* **YouTube Shorts API:** الرفع باستخدام واجهة برمجة تطبيقات داتا يوتيوب (YouTube Data API v3) مع تعيين وسم `privacyStatus` كـ `public` وتصنيف الفيديو كـ `Shorts`.
* **TikTok Graph API:** استخدام واجهة الرفع الرسمية لإرسال الفيديو مباشرة مضافاً إليه الوصف والوسوم المولدة.

## 🔑 2. إدارة الرموز اللامركزية (OAuth Token Management)
* لضمان عدم توقف النظام المستقل، يتم دمج سكريبت لتحديث الرموز آلياً (Refresh Tokens Tracker). 
* قبل كل عملية رفع بـ 5 دقائق، يتم التحقق من صلاحية رمز الوصول (Access Token) وتجديده تلقائياً دون أي تدخل بشري لضمان استمرارية النشر 24/7.
* بمجرد إتمام الرفع، يسحب الموديول الـ Video ID من الرد الرقمي للمنصة ويمرره فوراً لملف `database_schema.py` لتحديث البيانات.
