# وثيقة وحدة النشر والتوزيع التلقائي (publishing_module.md)

تحدد هذه الوثيقة المعمارية البرمجية، آليات الربط الشبكي، والمسؤوليات التنفيذية الموكلة إلى "وحدة النشر والتوزيع التلقائي" (Publishing & Distribution Module). تمثل هذه الوحدة المحطة التشغيلية الأخيرة لملف الفيديو، وتتولى مسؤولية الاتصال بالواجهات البرمجية (APIs) لمنصات التواصل الاجتماعي لرفع ونشر الفيديو النهائي، بالإضافة إلى إطلاق إشارة الحسم التطهيري للنظام فور إتمام النشر بنجاح.

---

## 🚀 آليات الرفع والربط مع المنصات (مستوحاة من الملف القديم)

تعمل الوحدة كمنفذ تقني لتوزيع المحتوى وفقاً لأعلى معايير الاستقرار الشبكي، وتنفذ عملياتها عبر طبقتين:

### 1. طبقة الرفع المتعدد وحقن النصوص الجانبية
* الاتصال البرمجي الآمن بـ APIs منصات (تيك توك، يوتيوب Shorts، وتويتر) باستخدام توكنز المصادقة المشفرة المخزنة في النظام.
* دمج العنوان البشري المقروء (Title) والهاشتاغات المستخرجة بناءً على الترند المستهدف، ورفعها كميتا داتا مصاحبة للفيديو لرفع نسب النشر والوصول (Reach Optimization).

### 2. إدارة الجلسات ومقاومة انقطاع الرفع
* تفعيل بروتوكول إعادة المحاولة الذكي (Exponential Backoff) في حال حدوث تذبذب في سرعة إنترنت السيرفر المستأجر، لضمان استكمال رفع الفيديو دون تلف الملف أو تكرار رفعه.
* التحقق من قيود وأحجام الملفات الخاصة بكل منصة وتعديل الطلب (API Request Body) ليتوافق مع شروط كل منصة تلقائياً.

---

## ⚙️ التحديث الهيكلي والربط مع المعمارية الجديدة (النقاط الخمس)

تمت إعادة تصميم النواة البرمجية لهذه الوحدة لتتكامل مع قاعدة البيانات وتكون هي المفتاح الرئيسي لإطلاق بروتوكول تصفير الذاكرة وتطهير السيرفر:

### 1. اشتراط فحص التوقيع الرقمي (C2PA Hard Lock)
* تم قفل هذه الوحدة برمجياً بحيث يحظر عليها البدء برفع أي فيديو إلا بعد استلام وسم الموافقة النهائي والختم الرقمي من ملف `c2pa_validation.md`. هذا يضمن عدم نشر أي فيديو غير محمي أو قد ترفضه خوارزميات المنصات.

### 2. إطلاق إشارة النجاح والتوقيع الزمني (Publish Trigger)
* فور اكتمال رفع الفيديو بنجاح على المنصات وحصول الوحدة على استجابة النجاح (HTTP 201 Created) من السيرفرات الخارجية، تقوم الوحدة فورا بـ:
  1. إرسال أمر للوكيل الإداري الأعلى لتسجيل التوقيع الزمني الكامل (Timestamp) ومعرف الفيديو (Video ID) داخل قاعدة البيانات `database_schema.py` لإصدار شهادة الفحص النهائية.
  2. تسجيل بصمة النص `SHA-256` رسمياً في جدول المنع لحماية القناة من تكرار المحتوى والحظر مستقبلاً.

### 3. تفعيل بروتوكول التطهير وحذف الفيديو (Post-Publish Wipe)
* فور إتمام النشر الناجح وإصدار الشهادة، تطلق هذه الوحدة "إشارة التطهير الحتمية" إلى وكيل الإنتاج (`production_agent.md`) وبروتوكول الجودة (`learning_realism_protocol.md`).
* بناءً على هذه الإشارة، يقوم النظام فوراً بحذف ملف الفيديو النهائي، واللقطات الخام، وملفات الصوت المؤقتة من الهارد ديسك وتفريغ كاش ذاكرة كرت الشاشة (VRAM)، محققاً معادلة (أعلى كفاءة انتشار × صفر استهلاك مساحة للسيرفر).

### 4. التعامل مع أوامر الإلغاء السامية
* إذا استقبلت هذه الوحدة إشارة تفيد بصدور أمر `الغاء` أو `رفض` من مقامكم السامي عبر التيليجرام أثناء مرحلة التجهيز، تقوم الوحدة فوراً بإلغاء جدولة الرفع، وإغلاق جلسات الاتصال المفتوحة، وتفويض ملف التوقيع الرقمي بمباشرة عملية التدمير الفوري لكافة ملفات الكاش لتصفيير السيرفر بالكامل لتريند جديد.
