# 🗑️ بروتوكول مسح البيانات وتنظيف النظام (Post-Publish Wipe Protocol)

## 📌 1. دواعي التفعيل والمحفزات (Execution Triggers)

يتم استدعاء وتنفيذ هذا البرتوكول برمجياً عبر النظام في حالتين فقط لا غير:

1. **النجاح والنشر (SUCCESSFUL DEPLOYMENT):** يتم التفعيل تلقائياً فور استلام إشارة تأكيد النشر الناجح من موديل النشر `publishing_module.txt` (سواء تم النشر بضغط زر الموافقة البشري أو بالنشر الذاتي التلقائي بعد انتهاء مهلة الـ 6 ساعات).
2. **الرفض القاطع والقتل (EXPLICIT DEVELOPER REJECTION):** يتم التفعيل فوراً وبشكل مستعجل عند قيام المطور بضغط زر `👎 مرفوض وحذف` عبر بوت التليجرام.

---

## 🛠️ 2. خطوات التنفيذ التقنية المتتالية (Technical Execution Steps)

عند تفعيل البروتوكول، يتعين على الحاوية البرمجية تنفيذ الخطوات التالية بالتتابع الصارم لضمان عدم ضياع السجلات:

### الخطوة 0: أرشفة سجل الامتثال (Compliance Receipt Audit) - [إجباري قبل المسح]
* **الإجراء:** قبل البدء بأي عملية مسح ميكانيكي للملفات، يجب على النظام توليد "إيصال الامتثال الرقمي المختوم".
* **الربط الرقمي:** إنشاء ملف أرشيف يربط بشكل دائم بين معرف الفيديو الرقمي `video_id` ومعرف المشروع `Project_ID`.
* **محتوى السجل:** استخراج وحفظ لقطة توثيقية (Audit Logs) تحتوي على:
  1. تقرير موافقة وكيل الرقابة الشرعية (Islamic Compliance Agent) وخلو المحتوى من المخالفات.
  2. تقرير فحص الامتثال لقانون الذكاء الاصطناعي الأوروبي (EU Law Agent) ورخص المشاع الإبداعي المستخدمة.
* **المستودع:** ترحيل هذا السجل فوراً إلى جدول الأرشفة الآمن في قاعدة البيانات (`database_schema.py`) تحت حالة `Archived & Certified` ليكون مرجعاً قانونياً دائماً لك ضد بلاغات المنصات، ولا يتم حذفه نهائياً تحت أي ظرف.

### الخطوة الأولى: إنهاء العمليات المعلقة (Process Termination)
* فحص معرّف المشروع الحالي `Project_ID`.
* إنهاء فورى لجميع عمليات الرندرة أو المعالجة الصوتية النشطة (FFmpeg / MoviePy / Edge-TTS) المرتبطة بهذا المعرّف في الخلفية لمنع استهلاك المعالج (CPU).

### الخطوة الثانية: تدمير الملفات المؤقتة والمسودات (Workspace Shredding)
* مسح المجلد المؤقت بالكامل الخاص بصناعة اللقطات الخام: `outputs/temp/{Project_ID}/`.
* تدمير كافة ملفات الصوت الأولية المستخرجة وملفات التزامن الشفهي (Lip-sync Cache).

### الخطوة الثالثة: تصفية المخرجات النهائية (Final Asset Handling)
* **في حالة الرفض:** يتم فوراً حذف ملف الفيديو النهائي المخرج `final_{Project_ID}.mp4` ميكانيكياً من السيرفر، وتغيير حالته في قاعدة البيانات إلى `Purged` لحظر أي محاولة نشر مستقبلي.
* **في حالة النشر الناجح:** يتم التأكد من نقل الفيديو إلى أرشيف النشر الخارجي، ثم تنظيف بيئة العمل المحلية لتوفير مساحة القرص الصلب للمشروع القادم.

### الخطوة الرابعة: تحرير الذاكرة العشوائية (RAM Garbage Collection)
* استدعاء دالة `gc.collect()` داخل بايثون بشكل إجباري لتفريغ كاش الذاكرة العشوائية الذي تستهلكه مكتبات معالجة الفيديو، وضمان استقرار حاوية التليجرام والنظام (Docker container) على المدى الطويل.
